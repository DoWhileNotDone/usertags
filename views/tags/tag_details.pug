extends ../layout

block content
  h1= title
  if tag
    p #{tag.name}
    | 
    a(href='/tags/' + tag.id + '/edit') Edit
    | 
    a(href='/tags/' + tag.id + '/delete') Delete
  else
    p No Tag Selected
  |
  a(id='link-tags-manage' href='/tags/') Tags
